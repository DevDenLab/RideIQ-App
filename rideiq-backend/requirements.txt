# pgmpy 1.1.2 needs numpy>=2.0, so the whole scientific stack is on numpy 2.x.
numpy>=2.0,<3
scikit-learn>=1.5
scipy>=1.13
matplotlib>=3.8
hmmlearn>=0.3.2
pgmpy==1.1.2
networkx>=3.2

fastapi>=0.111,<0.113
uvicorn[standard]>=0.30
pydantic>=2.7
redis>=5.0

# Transit itineraries are timestamped in America/Edmonton. python:3.11-slim ships
# without a system tz database, so zoneinfo needs this to resolve the zone at all.
tzdata>=2024.1
